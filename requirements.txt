# LLM client (OpenAI-compatible API для Yandex AI Studio)
openai==2.44.0

# Environment & HTTP
python-dotenv==1.0.1
requests==2.32.3

# Web UI
streamlit==1.58.0

# PDF-экспорт (markdown → HTML → PDF через xhtml2pdf, кириллица через DejaVu)
xhtml2pdf==0.2.17
markdown==3.10.2
